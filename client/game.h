#ifndef GAME_H
#define GAME_H

#include <enet/enet.h>

/*
void game_loop(ENetPeer * peer) {
	char * str = check_box_input();
	trim_whitespace(str);
	post_message(username, str);
	send_packet(peer, str);
}*/

#endif
